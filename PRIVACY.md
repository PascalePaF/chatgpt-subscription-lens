# 隐私说明

最后更新：2026-09-19

订阅镜 v1.0.0 的设计目标是让敏感会话凭证不经过第三方查询服务器。

## 数据处理

- 凭证只在当前进程内存中处理。
- 程序不创建账号、不提供云端服务、不含遥测、广告、崩溃上报或访问统计。
- 程序不把 Session JSON、Access Token、邮箱、账号 ID、订阅结果或账单写入文件。
- 查询成功后，前端输入框立即清空；输入闲置 5 分钟也会自动清空。
- 原始输入与提取出的 Access Token 在 Rust 侧使用尽力而为的内存清零。操作系统、WebView 和语言运行时仍可能产生不可控的临时内存副本，因此不要在不受信任的电脑上查询。

## 网络连接

应用业务代码只向 `https://chatgpt.com` 发送请求。请求用于：

- 用 session token 换取当前 Session JSON；
- 读取当前账号和权益；
- 读取订阅周期；
- 读取网页账单；
- 读取 Codex 使用额度。

HTTP 客户端只允许 HTTPS、拒绝跳转，目标 URL 固定在已编译的 Rust 代码中。应用不连接本项目作者的服务器。

## 本地文件

程序使用系统 WebView2 显示界面。WebView 以隐私模式运行，并把必要运行数据固定到可执行文件旁的 `subscription-lens-data/WebView2`。程序设置、查询历史和凭证不会另行保存。删除解压目录即可移除程序及其运行数据。

## 第三方边界

请求抵达 ChatGPT 后，由 OpenAI 按其隐私政策处理。订阅镜无法控制 OpenAI 的服务器日志、风控与数据保留。Apple/Google 的完整商店记录不经本程序读取。

## 用户责任

只应查询自己的账号，或已获得账号所有者明确授权的账号。不要把凭证粘贴到在线查询站、公开 Issue、聊天群、日志或截图中。
