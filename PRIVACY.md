# 隐私说明

订阅镜不运营服务器，不收集遥测，不使用广告 SDK，也不建立账户数据库。

## 处理的数据

用户主动粘贴的 Session JSON、Session Token、Access Token 或 Codex auth.json 仅用于本次进程内的只读查询。应用会在内存中解析令牌并向 `https://chatgpt.com` 发送授权请求；不会把它们发送给项目作者、GitHub、分析平台或其他第三方。

查询结果可能包含邮箱、脱敏账户 ID、套餐、时间、币种、卡品牌/尾号和网页账单摘要。这些结果仅显示在当前窗口，不写入磁盘。

## 网络请求

应用只允许 HTTPS 的 `chatgpt.com:443`，只发起 GET 请求，且禁用自动重定向。Apple/Google 商店历史不会由应用代查。

## 日志与崩溃报告

应用没有文件日志、自动崩溃上传或遥测模块。错误提示只显示经过归类的状态，不显示服务原始响应体或 Token。

## 用户责任

Session 与 Token 等同密码。只应查询自己有权访问的账户，不要把真实凭证放进 Issue、截图或反馈文本。关闭应用或完成查询后，操作系统仍可能在剪贴板中保留用户复制的内容；如有需要请自行覆盖剪贴板。
