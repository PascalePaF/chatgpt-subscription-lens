# 更新记录

## 1.0.1 — 2026-09-26

- 根据 Windows 工作区与 DPI 等比缩放整张原生界面，小屏仍不产生页面滚动；
- 对 HTTP 200 错误信封和缺失核心字段做逐端点语义校验，避免误报“读取成功”；
- JWT 新增签名段、`nbf`、`iat` 与极端时间戳检查；
- 支持根级默认支付方式 ID 和嵌套支付方式结构；
- 按币种最小单位正确换算零位/两位/三位小数账单，避免 JPY、KWD 等金额偏差；
- 改为多文件自包含安装，运行时组件全部位于安装目录，不再使用临时自解压；
- 升级时清理 V1.0.0 留下的可重建 `.net\SubscriptionLens` 临时运行缓存；
- 安装器完整清理旧版残留，卸载覆盖自包含运行时文件；
- CI Actions 升级到 Node 24 版本；
- 自动化覆盖扩展到高 DPI、随机凭证、错误信封、超大响应和支付结构漂移。
- 43 项离线自动化测试、全规则静态分析、安装/卸载/重装和已安装程序自检全部通过。

## 1.0.0 — 2026-09-26

- 从空仓库内容重新实现，不沿用旧版界面与代码结构；
- 改为 .NET 8 WPF 原生 Windows 桌面软件，无 WebView；
- 固定单窗口、无页面滚动、三行高度凭证输入；
- 支持 Session JSON、Session Token/Cookie、Access Token、Codex auth.json；
- 严格本地完整性检查与远端 Session 二次核验；
- 只读查询账户、订阅、Codex 额度、网页账单和支付方式；
- 套餐四色与黑色支付/商店卡片；
- 端点部分失败不覆盖其他有效结果；
- 首批 26 项自动化测试与 Windows 安装程序。
