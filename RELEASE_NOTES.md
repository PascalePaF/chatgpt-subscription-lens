# 订阅镜 v1.1.2

V1.1.2 修复了粘贴超长 Session JSON 或 token 后，输入控件撑满整个窗口并遮住查询按钮的问题。

## 修复内容

- Session 输入区现在是硬性三行高的独立视口，内容无论多长都不会改变窗口布局。
- 超出视口的内容只在输入框内滚动，会话文本仍保持完整，不会被截断后再查询。
- 完整性状态、账户授权勾选框和“开始只读查询”按钮始终固定在可见区域，可正常点击。
- 新增超长单行凭证的界面回归测试，直接验证输入框与后续按钮不会发生交互区域重叠。

## 保持不变

- 仍是 Rust + eframe/egui 的 Windows 原生桌面程序，不使用 WebView。
- 仍只执行固定 `chatgpt.com` 端点的只读查询，凭证不写入磁盘。
- 套餐配色、卡片正反面、支付图标、额度与账单布局保持不变。

## 验证

- 15 项 Rust 测试全部通过。
- `cargo fmt` 与 `cargo clippy -D warnings` 通过。
- 已执行 V1.1.1 到 V1.1.2 的覆盖安装、版本资源、原生窗口启动与无 WebView 子进程检查。

下载 `SubscriptionLens-v1.1.2-windows-x64-setup.exe`，校验值见同一 Release 的 `SHA256SUMS.txt`。
