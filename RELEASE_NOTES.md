# 订阅镜 1.0.1

这是项目推倒重建后的首个加固版本。

它保留 V1.0.0 的 Windows WPF 原生界面、三行输入、GET-only 查询和订阅/账单/Codex 额度卡片，并修复首轮自审发现的问题：高 DPI 与小屏适配、HTTP 200 错误信封误判、JWT 边界、默认银行卡选择以及单文件运行时临时解压。

V1.0.1 改为安装器内的多文件自包含发布；程序与 .NET 运行组件全部安装到 `%LOCALAPPDATA%\Programs\SubscriptionLens`，用户仍无需另装 .NET。能够确认的数据继续显示，OpenAI 没有独立只读接口的额度继续明确标为未返回。

发布前已完成 43 项离线自动化测试、最高级别 .NET 静态分析，以及安装、卸载、重装、安装后核心自检和界面布局自检。
