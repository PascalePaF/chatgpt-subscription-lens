# 订阅镜 1.0.0

这是项目推倒重建后的第一个正式版本。

它是一款真正的 Windows WPF 桌面应用：没有 WebView，没有浏览器式长页面，也不会让大段 Session JSON 撑满窗口。程序按当前用户安装到 `%LOCALAPPDATA%\Programs\SubscriptionLens`，带开始菜单快捷方式与卸载程序。

V1.0.0 聚焦“本地、安全、诚实”：能够确认的订阅、支付来源、网页账单和 Codex 额度会显示；OpenAI 没有提供独立只读计数的 Chat、生图和 Deep Research 会明确标为未返回，而不是猜一个数字。
